spring.application.name=RestApi

# Server config
server.port=8080
server.servlet.context-path=/api/v1

# Supabase PostgreSQL (use environment variables)
spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://db.wdnojjtzkkadujlnkfvi.supabase.co:5432/postgres}
spring.datasource.username=${SUPABASE_DB_USER:postgres.wdnojjtzkkadujlnkfvi}
spring.datasource.password=${SUPABASE_DB_PASSWORD:Jinu@123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA config for PostgreSQL
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret
jwt.secret=${JWT_SECRET:expensemanager}

# Connection pool configuration for production
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
